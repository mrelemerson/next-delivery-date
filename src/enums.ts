export enum ScheduleType {
  F = 'None',
  W = 'Week',
  M = 'Month',
}

export enum FrequencyType {
  D = 'day',
  W = 'week',
  M = 'month',
  S = 'schedule',
}

export enum FirstDeliveryType {
  F,
  S,
}
